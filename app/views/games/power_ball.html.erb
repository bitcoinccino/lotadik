<!DOCTYPE html>
<html>
<head>
	<title>Pick Your Games</title>
</head>
<body>
  <div class="container">
	<div class="jumbotron one">
	    <h3 class="be_rich">May you be fucking rich forever, <%= $site_name %></h3><br>
		<p class="numbers"><i>Here's your winning <strong>Powerball</strong> numbers for <%= $the_date.to_formatted_s(:long_ordinal) %></i></p>
		<h3 class="be_rich"><span><%= @five_numbers + " and " + @power_ball %></span></h3><hr>
    <%= render "layouts/lotto_site" %>
     </div>
</div>


<%= render "layouts/comments" %>

<%= render "layouts/footer" %>

</body>
</html>
